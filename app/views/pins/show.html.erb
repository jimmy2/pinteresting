<%= image_tag @pin.image.url, class: "img-responsive" %>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<% if user_signed_in? && @pin.user == current_user %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>

<%= link_to 'Back', pins_path %>
